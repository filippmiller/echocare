# Отчет о реализации - Profile MVP + Journal MVP

**Ветка:** `feat/profile-mvp`  
**Дата:** 12 ноября 2025  
**Статус:** ✅ Готово к PR

---

## 📋 ВЫПОЛНЕННЫЕ ЗАДАЧИ

### ФАЗА 1: Срочные правки и подготовка ✅

#### 1.1 Supabase клиент (server-only)
- ✅ Добавлен `@supabase/supabase-js@^2.45.4`
- ✅ Создан `src/lib/supabaseServer.ts` с админ-клиентом
- ✅ Экспортированы константы: `JOURNAL_AUDIO_BUCKET`, `MAX_AUDIO_BYTES`, `ALLOWED_AUDIO_MIME`

#### 1.2 Стандартизатор ошибок API
- ✅ Создан `src/lib/apiErrors.ts` с хелперами (400, 401, 403, 404, 413, 415, 500)
- ✅ Обновлен `src/app/api/auth/register/route.ts` для использования новых хелперов

#### 1.3 Документация
- ✅ Обновлен `CREDENTIALS.md` с новыми env переменными
- ✅ Создан `SETUP_STORAGE.md` с инструкциями по bucket
- ✅ Создан `RAILWAY_MIGRATIONS.md` с инструкциями по миграциям

#### 1.4 Production deploy hook
- ✅ Обновлен `package.json` скрипт `start`: `pnpm prisma migrate deploy && next start`

---

### ФАЗА 2: Профиль пользователя (MVP) ✅

#### 2.1 Prisma Schema
- ✅ Добавлен enum `Gender` (MALE, FEMALE, OTHER, UNKNOWN)
- ✅ Добавлена модель `Profile` с полями: `id`, `userId`, `fullName`, `birthDate`, `city`, `phone`, `locale`, `timezone`, `gender`
- ✅ Создана миграция `20251111214131_add_profile`

#### 2.2 API эндпоинты
- ✅ `GET /api/profile` - возвращает профиль (создает пустой при первом запросе)
- ✅ `PUT /api/profile` - обновляет профиль с валидацией Zod
- ✅ Проверка прав доступа (только owner через `session.user.id`)

#### 2.3 Валидация
- ✅ Создан `src/lib/validations/profile.ts` с Zod схемой

#### 2.4 UI
- ✅ Создан компонент `ProfileForm` с формой редактирования
- ✅ Обновлен `/dashboard` для SSR загрузки и отображения профиля

**Коммит:** `cursor: phase-2 profile mvp (schema+api+ui) + apiErrors + supabaseServer`

---

### ФАЗА 3: Журнал (текст + аудио) - MVP ✅

#### 3.1 Prisma Schema
- ✅ Добавлены enum `JobStatus` (PENDING, PROCESSING, DONE, FAILED)
- ✅ Добавлены enum `EntryType` (TEXT, AUDIO)
- ✅ Добавлена модель `AudioAsset` с полями: `id`, `userId`, `bucket`, `path`, `mime`, `size`, `duration`
- ✅ Добавлена модель `JournalEntry` с полями: `id`, `userId`, `type`, `title`, `text`, `mood`, `energy`, `tags`, `audioId`
- ✅ Добавлена модель `Job` для будущей транскрипции
- ✅ Создана миграция `20251111222336_add_journal_models`

**Коммит:** `cursor: journal schema (AudioAsset, JournalEntry, Job) + enums`

#### 3.2 API эндпоинты

**POST /api/journal/entries**
- ✅ Создает текстовую запись (type=TEXT)
- ✅ Валидация через Zod (`createEntrySchema`)
- ✅ Поля: `title?`, `text`, `mood?`, `energy?`, `tags?`
- ✅ Проверка прав доступа (только owner)

**GET /api/journal/entries**
- ✅ Пагинация cursor-based (`?limit=20&cursor=...`)
- ✅ Возвращает только записи текущего пользователя
- ✅ Включает связанный `audio` объект
- ✅ Возвращает `nextCursor` и `hasMore`

**POST /api/journal/upload**
- ✅ Принимает `multipart/form-data` с полем `file`
- ✅ Проверка размера ≤ MAX_AUDIO_MB (20MB)
- ✅ Проверка MIME типа ∈ ALLOWED_AUDIO_MIME
- ✅ Проверка существования bucket (503 если отсутствует)
- ✅ Загрузка в Supabase Storage по пути: `user/<userId>/<yyyy>/<mm>/<dd>/<cuid>.<ext>`
- ✅ Создание `AudioAsset` и `JournalEntry` (type=AUDIO)
- ✅ Ошибки: 401 (нет сессии), 413 (размер), 415 (MIME), 503 (нет bucket), 400 (валидация)

**Коммит:** `cursor: journal API (create/list/upload) with size/MIME checks and 401/413/415/503`

#### 3.3 UI на /dashboard

**Компонент `NewEntryForm`:**
- ✅ Textarea для текста
- ✅ Поля: title, text, mood, energy
- ✅ Кнопка Save → POST /api/journal/entries
- ✅ Автоматическое обновление списка после создания

**Компонент `AudioRecorder`:**
- ✅ Кнопки Record/Stop (MediaRecorder API)
- ✅ Запись аудио в браузере
- ✅ Отправка FormData → POST /api/journal/upload
- ✅ Индикация записи и загрузки

**Компонент `JournalEntriesList`:**
- ✅ Список последних записей с SSR
- ✅ Отображение типа (📝 Text / 🎤 Audio)
- ✅ Дата создания
- ✅ Превью текста (первые 120 символов) или "Audio note (N sec)"
- ✅ Пагинация cursor-based (Load More)
- ✅ Кнопка Refresh

**Коммит:** `cursor: dashboard minimal journal UI (text entry, audio record/upload, list)`

---

## 📊 СТАТИСТИКА

### Файлы:
- **Создано:** 15 новых файлов
- **Изменено:** 8 существующих файлов
- **Миграции:** 2 созданы (Profile, Journal)

### Коммиты:
1. `cursor: phase-2 profile mvp (schema+api+ui) + apiErrors + supabaseServer`
2. `cursor: prod migrate deploy hook + storage bucket setup docs`
3. `cursor: journal schema (AudioAsset, JournalEntry, Job) + enums`
4. `cursor: journal API (create/list/upload) with size/MIME checks and 401/413/415/503`
5. `cursor: dashboard minimal journal UI (text entry, audio record/upload, list)`
6. `cursor: fix dashboard refresh handlers`

---

## 🔍 НОВЫЕ API ЭНДПОИНТЫ

### Профиль
- `GET /api/profile` - получить профиль текущего пользователя
- `PUT /api/profile` - обновить профиль

### Журнал
- `POST /api/journal/entries` - создать текстовую запись
- `GET /api/journal/entries?limit=20&cursor=...` - получить список записей (пагинация)
- `POST /api/journal/upload` - загрузить аудио файл

---

## ⚠️ ТРЕБУЕТ ВНИМАНИЯ (Перед PR)

### 1. Применить миграции на Railway
**Действие:** Выполнить `pnpm prisma migrate deploy` на Railway

**Миграции для применения:**
- `20251111232818_add_phone_field` (если еще не применена)
- `20251111214131_add_profile`
- `20251111222336_add_journal_models`

**Инструкции:** См. `RAILWAY_MIGRATIONS.md`

### 2. Создать Storage bucket
**Действие:** Создать bucket `journal-audio` в Supabase Dashboard

**Настройки:**
- Имя: `journal-audio`
- Тип: **Private** (не публичный)
- Никаких публичных правил

**Инструкции:** См. `SETUP_STORAGE.md`

### 3. Проверить переменные окружения в Railway
Убедиться, что заданы:
- `NEXT_PUBLIC_SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_BUCKET=journal-audio`
- `ALLOWED_AUDIO_MIME=audio/webm,audio/ogg,audio/m4a,audio/mp3`
- `MAX_AUDIO_MB=20`

---

## ✅ VERIFY ЧЕКЛИСТ (После применения миграций и создания bucket)

### Базовые функции
- [ ] Регистрация/логин работает
- [ ] `/dashboard` доступен после логина
- [ ] Профиль загружается SSR и отображается
- [ ] Профиль сохраняется через PUT /api/profile

### Журнал - текст
- [ ] Текстовая запись создается через форму
- [ ] Запись появляется в списке сразу после создания
- [ ] Отображается правильная дата и тип
- [ ] Превью текста работает (первые 120 символов)

### Журнал - аудио
- [ ] Кнопка Record запрашивает разрешение микрофона
- [ ] Запись останавливается по кнопке Stop
- [ ] Аудио загружается на сервер (5-10 сек тест)
- [ ] Файл появляется в Supabase Storage по правильному пути
- [ ] В БД создается `AudioAsset` и `JournalEntry`
- [ ] Запись отображается в списке как "Audio note"

### Безопасность API
- [ ] Неавторизованный вызов API → 401
- [ ] Неверный MIME тип → 415
- [ ] Превышение размера файла → 413
- [ ] Отсутствие bucket → 503
- [ ] Логи не содержат секретов
- [ ] Service Role Key не утекает на клиент

### Пагинация
- [ ] Load More загружает следующие записи
- [ ] Cursor-based пагинация работает корректно
- [ ] Кнопка Refresh обновляет список

---

## 📝 ЗАМЕТКИ

### Технические детали:
- Все API роуты используют стандартизированные ошибки из `apiErrors.ts`
- Supabase клиент используется только на сервере (никогда на клиенте)
- MediaRecorder API работает только в HTTPS или localhost
- Пагинация использует cursor-based подход (более эффективно чем offset)

### Известные ограничения:
- Аудио запись работает только в браузерах с поддержкой MediaRecorder
- Требуется HTTPS для доступа к микрофону (кроме localhost)
- Транскрипция аудио отложена (Job создается, но воркер не реализован)

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Применить миграции на Railway** (см. `RAILWAY_MIGRATIONS.md`)
2. **Создать bucket в Supabase** (см. `SETUP_STORAGE.md`)
3. **Проверить env переменные в Railway**
4. **Провести VERIFY тесты** (чеклист выше)
5. **Открыть PR** из ветки `feat/profile-mvp` в `main`

---

**Готово к PR после применения миграций и создания bucket!** ✅

