# HOTFIX: Runtime без pnpm — Инструкция по применению

## ✅ Что сделано в коде

1. ✅ Обновлен `package.json`:
   - `start` скрипт теперь использует `npx` вместо `pnpm`
   - Миграции: `npx prisma migrate deploy`
   - Next.js старт: `npx next start`

2. ✅ Обновлен `Procfile`:
   - Изменено с `web: pnpm start` на `web: npm start`

3. ✅ Упрощен `nixpacks.toml`:
   - Убрана секция `[start]` (теперь используется Procfile)
   - Build по-прежнему использует pnpm (через nixpacks)

4. ✅ Ветка `fix/runtime-without-pnpm` создана и запушена

## 📋 Следующие шаги в Railway Dashboard

### 1. Обнови Start Command (если нужно)

**Railway Dashboard → [Service] → Settings → Deploy**

- **Start Command:** `npm start` (или оставь пустым — Procfile подхватится автоматически)
- **Build Command:** оставить пустым (используется из `nixpacks.toml`)

### 2. Подключи ветку для деплоя

**Railway Dashboard → [Service] → Settings → Source**
- Выбери ветку: `fix/runtime-without-pnpm`
- Или сделай Redeploy после merge в `main`

### 3. Запусти Redeploy

**Railway Dashboard → [Service] → Deployments → [Latest] → Redeploy**

## 🔍 Проверка после деплоя

### 1. Runtime Logs

**Railway Dashboard → [Service] → Deployments → [Latest] → Runtime Logs**

Должны появиться логи:
```
Running migrations...
Starting Next.js on PORT=8080
```

**⚠️ Если видишь `pnpm: command not found`** — значит Railway всё ещё использует старую конфигурацию. Проверь:
- Start Command в Settings → Deploy
- Procfile действительно обновлён в репо

### 2. Health Check

Открой в браузере:
```
https://echocare-production.up.railway.app/api/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "ts": "2025-11-12T...",
  "port": "8080"
}
```

### 3. Основные страницы

- `https://echocare-production.up.railway.app/` — главная
- `https://echocare-production.up.railway.app/dashboard` — дашборд (после логина)
- `https://echocare-production.up.railway.app/api/profile` — API профиля

## 🚨 План Б (если всё ещё 404)

### Временно убери миграции из старта

Если миграции мешают запуску, временно убери их из `start` скрипта:

**package.json:**
```json
{
  "start": "echo \"Starting Next.js on PORT=${PORT:-3000}\" && npx next start -p ${PORT:-3000}"
}
```

Затем выполни миграции отдельно через Railway CLI:
```bash
railway run npx prisma migrate deploy
```

### Проверь Service Type

**Railway Dashboard → [Service] → Settings → General**
- Убедись, что тип сервиса: **Web Service** (не Static)

## 📝 Чек-лист успешного деплоя

- [ ] В Runtime Logs видны echo-сообщения от start скрипта
- [ ] `/api/health` отдаёт `{ ok: true }`
- [ ] Главная страница открывается без 404
- [ ] Приложение отвечает на запросы
- [ ] Нет ошибок `pnpm: command not found`

## 💡 Как это работает

**Build фаза (через nixpacks.toml):**
- Устанавливает pnpm через Nix
- Использует `pnpm install --frozen-lockfile` для установки зависимостей
- Использует `pnpm run build` для сборки

**Runtime фаза (через Procfile):**
- Использует `npm start` (npm всегда доступен в Node.js контейнере)
- `npm start` вызывает скрипт из `package.json`
- Скрипт использует `npx` для запуска prisma и next (npx всегда доступен)

**Преимущества:**
- ✅ Build использует pnpm (быстрее, эффективнее)
- ✅ Runtime не зависит от pnpm (npm/npx всегда доступны)
- ✅ Нет проблем с установкой pnpm в runtime окружении

---

**Ветка:** `fix/runtime-without-pnpm`  
**Коммит:** `94cbdba`  
**Дата:** 12 ноября 2025


