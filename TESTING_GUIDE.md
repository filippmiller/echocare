# Полный обзор изменений и руководство по тестированию

**Дата:** 2025-11-12  
**Ветка:** `feat/ui-refresh-nav-lang` (merged with `feat/admin-api-keys`)  
**Статус:** 🚀 Готово к тестированию

---

## 📋 Полный список изменений

### 1. 🎤 Транскрипция аудио (Background Processing)

**Что добавлено:**
- Автоматическая транскрипция аудио записей через OpenAI Whisper API
- Фоновый worker для обработки очереди транскрипций
- Генерация summary и тегов из транскрипции через GPT-4o-mini
- Отслеживание статуса транскрипции (PENDING → RUNNING → DONE/ERROR)

**Файлы:**
- `prisma/schema.prisma` - добавлена модель `TranscriptionJob`, поля `summary`, `transcribedAt` в `JournalEntry`
- `src/lib/transcription.ts` - core функции транскрипции и генерации summary/tags
- `src/workers/transcription-worker.ts` - worker для обработки очереди
- `src/app/api/cron/transcription/route.ts` - cron endpoint для Railway
- `src/app/api/journal/transcribe/[entryId]/route.ts` - создание job транскрипции
- `src/app/api/journal/transcribe/status/[jobId]/route.ts` - проверка статуса

**Миграция БД:**
```bash
npx prisma migrate deploy
```

---

### 2. 🎯 QuickRecord - Быстрая запись (FAB)

**Что добавлено:**
- Плавающая кнопка записи (FAB) в правом нижнем углу
- Поддержка press&hold для записи
- Автоматическая загрузка и транскрипция после записи
- Визуальные индикаторы состояний (recording → uploading → transcribing → ready)

**Файлы:**
- `src/components/quick-record.tsx` - основной компонент FAB
- `src/app/dashboard/page.tsx` - интеграция QuickRecord

**UI состояния:**
- 🎤 **Idle** - кнопка готова к записи
- 🔴 **Recording** - идет запись (показывается таймер)
- ☁️ **Uploading** - загрузка на сервер
- ✍️ **Transcribing** - обработка транскрипции
- ✅ **Ready** - готово!

---

### 3. 🔍 Поиск и фильтры

**Что добавлено:**
- Полнотекстовый поиск по записям (text, summary, title)
- Фильтр по тегам (можно указать несколько через запятую)
- Фильтр по диапазону дат (from/to)
- Сохранение фильтров в localStorage
- Курсорная пагинация для больших результатов

**Файлы:**
- `src/app/api/journal/search/route.ts` - API endpoint поиска
- `src/components/journal-search.tsx` - форма поиска с фильтрами
- `src/components/journal-search-wrapper.tsx` - обертка с состоянием
- `src/components/journal-entries-list.tsx` - обновлен для поддержки поиска

**Использование:**
1. Ввести текст в поле поиска
2. Указать теги (через запятую): `work, personal`
3. Выбрать диапазон дат
4. Нажать "Search"
5. Экспортировать результаты (CSV/PDF)

---

### 4. 📤 Экспорт записей

**Что добавлено:**
- Экспорт в CSV формат
- Экспорт в PDF формат (с форматированием)
- Экспорт с учетом фильтров поиска
- Ограничение: максимум 1000 записей за раз

**Файлы:**
- `src/app/api/journal/export/route.ts` - API endpoint экспорта
- `src/components/journal-search.tsx` - кнопки экспорта (CSV/PDF)

**Формат CSV:**
- Колонки: ID, Created At, Type, Title, Text, Summary, Tags, Mood, Energy, Audio URL

**Формат PDF:**
- Красивое форматирование с датами, тегами, summary
- Поддержка длинных текстов с переносами

---

### 5. 👨‍💼 Админ-панель транскрипций

**Что добавлено:**
- Страница `/admin/transcriptions` для управления транскрипциями
- Статистика: Total, Done, Pending, Running, Errors
- Фильтрация по статусу
- Возможность retry для failed jobs
- Возможность cancel для pending/running jobs

**Файлы:**
- `src/app/admin/transcriptions/page.tsx` - страница админа
- `src/components/admin/transcription-jobs-list.tsx` - список jobs
- `src/app/api/admin/transcriptions/route.ts` - API список
- `src/app/api/admin/transcriptions/[jobId]/retry/route.ts` - retry endpoint
- `src/app/api/admin/transcriptions/[jobId]/cancel/route.ts` - cancel endpoint

---

### 6. 🔐 Управление API ключами (Admin)

**Что добавлено:**
- Хранение API ключей в БД (зашифрованные)
- UI для управления ключами через админ-панель
- Автоматическое использование ключей из БД вместо env vars
- Тестирование ключей перед сохранением
- Отслеживание последнего использования

**Файлы:**
- `prisma/schema.prisma` - модель `ApiKey`
- `src/lib/apiKeys.ts` - утилиты шифрования/дешифрования
- `src/app/admin/api-keys/page.tsx` - страница управления ключами
- `src/components/admin/api-keys-list.tsx` - список ключей
- `src/components/admin/api-keys-form.tsx` - форма создания/редактирования
- `src/app/api/admin/api-keys/*` - все API endpoints
- `src/lib/transcription.ts` - обновлен для использования ключей из БД

**Безопасность:**
- AES-256-GCM шифрование
- Ключи никогда не показываются полностью (только последние 4 символа)
- Fallback на `OPENAI_API_KEY` env var если нет ключа в БД

---

### 7. 🌐 Интернационализация (i18n)

**Что добавлено:**
- Поддержка русского и английского языков
- Переключатель языка в header
- Все новые фичи переведены

**Файлы:**
- `src/i18n/messages/en.json` - английские переводы
- `src/i18n/messages/ru.json` - русские переводы
- `src/components/layout/LanguageSwitcher.tsx` - переключатель языка

---

## 🧪 Пошаговое руководство по тестированию

### Шаг 1: Подготовка окружения

**1.1. Применить миграции БД:**
```bash
cd C:\dev\echocare\clear-mind-app
npx prisma migrate deploy
npx prisma generate
```

**1.2. Проверить переменные окружения (.env.local):**
```env
# Обязательные
DATABASE_URL=postgresql://...
NEXTAUTH_SECRET=...
NEXTAUTH_URL=http://localhost:3005
NEXT_PUBLIC_SUPABASE_URL=...
SUPABASE_SERVICE_ROLE_KEY=...

# Для транскрипции (можно через UI или env)
OPENAI_API_KEY=sk-...  # Fallback если нет ключа в БД

# Опционально
WHISPER_PROVIDER=openai
WHISPER_MODEL=gpt-4o-mini-transcribe
CRON_SECRET=your-secret-here
```

**1.3. Запустить dev сервер:**
```bash
npm run dev
# или
pnpm dev
```

---

### Шаг 2: Тестирование аутентификации и ролей

**2.1. Создать админ-пользователя:**
- Убедиться что `philippemiller@gmail.com` в `ADMIN_EMAILS` env var
- Зарегистрироваться с этим email
- Проверить что роль = ADMIN

**2.2. Проверить админ-доступ:**
- Войти как админ
- Проверить что в header есть ссылка "Admin"
- Перейти на `/admin` - должна открыться админ-панель

---

### Шаг 3: Тестирование управления API ключами

**3.1. Добавить OpenAI ключ:**
1. Перейти: `/admin/api-keys`
2. Нажать "Add New Key"
3. Заполнить форму:
   - Name: "OpenAI Production"
   - Provider: "openai"
   - Key Value: вставить реальный ключ `sk-...`
4. Нажать "Create"
5. Проверить что ключ появился в списке

**3.2. Протестировать ключ:**
1. Найти созданный ключ в списке
2. Нажать кнопку "Test"
3. Должно появиться сообщение: "Key is valid. Found X models available."

**3.3. Проверить маскирование:**
- В списке ключей должен показываться только последний символ: `••••xxxx`
- Полный ключ не должен быть виден

**3.4. Деактивировать/активировать:**
- Нажать "Deactivate" на активном ключе
- Проверить что статус изменился на "Inactive"
- Нажать "Activate" - статус должен вернуться

---

### Шаг 4: Тестирование QuickRecord (FAB)

**4.1. Базовая запись:**
1. Перейти на `/dashboard`
2. Найти плавающую кнопку с микрофоном в правом нижнем углу
3. Нажать и удерживать кнопку (или просто нажать для iOS)
4. Должен появиться индикатор "Recording" с таймером
5. Отпустить кнопку (или нажать снова для остановки)
6. Должен появиться индикатор "Uploading..."
7. Затем "Transcribing..."
8. В конце "Ready!"

**4.2. Проверить результат:**
- После завершения запись должна появиться в списке "MY ENTRIES"
- Должен быть виден статус транскрипции

**4.3. Проверить автоматическую транскрипцию:**
- Подождать 2-3 минуты (или запустить cron вручную)
- Обновить страницу
- Проверить что у записи появился текст транскрипции
- Проверить что появились summary и теги

---

### Шаг 5: Тестирование поиска и фильтров

**5.1. Создать тестовые записи:**
- Создать несколько текстовых записей с разными тегами
- Создать несколько аудио записей
- Добавить теги вручную (если есть UI для этого)

**5.2. Тест текстового поиска:**
1. В поле поиска ввести слово из существующей записи
2. Нажать "Search"
3. Должны отобразиться только записи содержащие это слово

**5.3. Тест фильтра по тегам:**
1. В поле "Tags" ввести тег (например: `work`)
2. Нажать "Search"
3. Должны отобразиться только записи с этим тегом

**5.4. Тест фильтра по датам:**
1. Выбрать дату "From" (например, вчера)
2. Выбрать дату "To" (сегодня)
3. Нажать "Search"
4. Должны отобразиться только записи в этом диапазоне

**5.5. Комбинированные фильтры:**
- Попробовать комбинацию: текст + теги + даты
- Проверить что все фильтры работают вместе

---

### Шаг 6: Тестирование экспорта

**6.1. Экспорт CSV:**
1. Применить фильтры поиска (или оставить пустым для всех записей)
2. Нажать кнопку "CSV"
3. Должен скачаться файл `journal-export-YYYY-MM-DD.csv`
4. Открыть в Excel/LibreOffice
5. Проверить что все колонки заполнены корректно

**6.2. Экспорт PDF:**
1. Применить фильтры поиска
2. Нажать кнопку "PDF"
3. Должен скачаться файл `journal-export-YYYY-MM-DD.pdf`
4. Открыть PDF
5. Проверить форматирование, наличие всех данных

**6.3. Экспорт с фильтрами:**
- Применить фильтры (например, только записи с тегом "work")
- Экспортировать
- Проверить что экспортированы только отфильтрованные записи

---

### Шаг 7: Тестирование транскрипции (Worker)

**7.1. Проверить создание TranscriptionJob:**
1. Загрузить аудио запись
2. Проверить в БД (Prisma Studio) что создался `TranscriptionJob` со статусом `PENDING`

**7.2. Запустить worker вручную:**
```bash
# Вариант 1: Через API endpoint (если настроен cron)
curl -X GET http://localhost:3005/api/cron/transcription \
  -H "Authorization: Bearer YOUR_CRON_SECRET"

# Вариант 2: Через Prisma Studio
# Найти PENDING job и изменить статус вручную (не рекомендуется)
```

**7.3. Проверить результат:**
- После обработки job должен иметь статус `DONE`
- В `JournalEntry` должны быть заполнены:
  - `text` - текст транскрипции
  - `summary` - краткое резюме
  - `tags` - автоматически сгенерированные теги
  - `transcribedAt` - дата завершения

**7.4. Проверить админ-панель транскрипций:**
1. Перейти `/admin/transcriptions`
2. Должны отображаться все jobs
3. Проверить статистику (Total, Done, Pending, etc.)
4. Попробовать фильтрацию по статусу

**7.5. Тест retry:**
- Найти failed job (или создать тестовый с ошибкой)
- Нажать "Retry"
- Проверить что статус изменился на `PENDING`

**7.6. Тест cancel:**
- Найти pending или running job
- Нажать "Cancel"
- Проверить что статус изменился на `ERROR` с сообщением "Cancelled by admin"

---

### Шаг 8: Тестирование интеграции API ключей с транскрипцией

**8.1. Проверить использование ключа из БД:**
1. Убедиться что есть активный OpenAI ключ в БД (`/admin/api-keys`)
2. Удалить или закомментировать `OPENAI_API_KEY` из `.env.local`
3. Загрузить аудио запись
4. Запустить worker транскрипции
5. Проверить что транскрипция работает (используется ключ из БД)

**8.2. Проверить fallback на env var:**
1. Деактивировать все ключи в БД
2. Восстановить `OPENAI_API_KEY` в `.env.local`
3. Загрузить аудио запись
4. Проверить что транскрипция работает (используется env var)

**8.3. Проверить отслеживание использования:**
1. Использовать ключ из БД для транскрипции
2. Проверить в `/admin/api-keys` что `lastUsedAt` обновился

---

### Шаг 9: Тестирование UI/UX

**9.1. Проверить навигацию:**
- Header должен показывать правильные ссылки для админа
- Переключатель языка должен работать
- Аватар должен отображаться корректно

**9.2. Проверить отзывчивость:**
- Тестировать на разных размерах экрана
- Проверить что FAB кнопка видна на мобильных
- Проверить что формы работают на touch устройствах

**9.3. Проверить ошибки:**
- Попробовать загрузить слишком большой файл
- Попробовать загрузить неподдерживаемый формат
- Проверить что показываются понятные сообщения об ошибках

---

## 🔍 Чеклист тестирования

### Базовые функции
- [ ] Регистрация и вход работают
- [ ] Админ-пользователь видит ссылку "Admin"
- [ ] Обычный пользователь не видит админ-ссылки

### API ключи
- [ ] Можно создать новый ключ
- [ ] Ключ шифруется в БД
- [ ] Можно протестировать ключ
- [ ] Можно активировать/деактивировать ключ
- [ ] Можно удалить ключ
- [ ] Ключ используется для транскрипции

### QuickRecord
- [ ] FAB кнопка видна на dashboard
- [ ] Можно начать запись (press&hold или tap)
- [ ] Показывается индикатор записи
- [ ] Можно остановить запись
- [ ] Запись загружается автоматически
- [ ] Запись появляется в списке

### Транскрипция
- [ ] TranscriptionJob создается при загрузке аудио
- [ ] Worker обрабатывает очередь
- [ ] Транскрипция завершается успешно
- [ ] Текст, summary и теги заполняются
- [ ] Статус обновляется корректно

### Поиск
- [ ] Текстовый поиск работает
- [ ] Фильтр по тегам работает
- [ ] Фильтр по датам работает
- [ ] Комбинированные фильтры работают
- [ ] Результаты обновляются динамически

### Экспорт
- [ ] CSV экспорт работает
- [ ] PDF экспорт работает
- [ ] Экспорт учитывает фильтры
- [ ] Файлы скачиваются корректно

### Админ-панель
- [ ] Страница транскрипций открывается
- [ ] Статистика отображается корректно
- [ ] Можно фильтровать по статусу
- [ ] Retry работает
- [ ] Cancel работает

---

## 🐛 Известные проблемы / Ограничения

1. **RLS политики:** Не настроены (требует ручной настройки в Supabase Dashboard)
2. **Full-text search:** Использует простой LIKE, можно улучшить через PostgreSQL tsvector
3. **Экспорт:** Максимум 1000 записей за раз (защита от злоупотребления)
4. **Транскрипция:** Максимум 3 попытки на job
5. **PDF форматирование:** Базовое, можно улучшить

---

## 📝 Следующие шаги после тестирования

1. Если все работает → создать PR
2. Если есть баги → исправить и закоммитить
3. Настроить Railway cron для автоматической обработки транскрипций
4. Настроить RLS политики в Supabase (опционально)
5. Добавить мониторинг и алерты (опционально)

---

**Удачи с тестированием! 🚀**

